import CompletedChallenges from "../components/completedChallenges";

const Home = () => {
  return (
    <div className="container">
      <CompletedChallenges />
    </div>
  );
};

export default Home;
